apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-identity
  namespace: gag
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /storage/data/postgres/identity